<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Color de fondo -->
    <stroke
        android:width="2dp"
        android:color="@color/lavender" /> <!-- Borde negro -->
    <corners android:radius="4dp" /> <!-- Bordes redondeados opcionales -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
</shape>
